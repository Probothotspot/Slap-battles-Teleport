return function(Tab)
    Tab:CreateSection("Первая страница")

    Tab:CreateParagraph({
        Title = "Привет",
        Content = "Этот текст подгружен из файла 1-й вкладки на GitHub!"
    })

    Tab:CreateButton({
        Name = "Кнопка из вкладки Привет",
        Callback = function()
            print("Сработала кнопка из 1-й вкладки!")
        end,
    })
end
